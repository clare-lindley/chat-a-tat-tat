{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "one of my first socket.io apps :D",
  "private": true,
  "scripts": {
    "build": "babel -d lib/ src"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
